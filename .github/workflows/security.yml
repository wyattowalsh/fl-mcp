name: Security
on:
  pull_request:
  push:
    branches: ["main", "work"]
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python -m pip install pip-audit
      - run: pip-audit || true
