[project]
name = "fl-mcp-provider-template"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fl-mcp>=0.1.0a0"]

[project.entry-points."fl_mcp.providers"]
template = "template_provider:provider"
template_provider = "template_provider:provider"
